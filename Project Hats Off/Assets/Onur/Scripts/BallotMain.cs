using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BallotMain : MonoBehaviour
{
    public int suspectSelected = 0;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void SetSuspectNumber(int number) 
    {
        suspectSelected = number;
    }
}
